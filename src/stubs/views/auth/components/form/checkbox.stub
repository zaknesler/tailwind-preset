<label class="flex items-center">
    <input
        type="checkbox"
        name="{{ $name }}"
        {{ $attributes->merge(['class' => 'form-checkbox focus:shadow-transition text-brand-500 cursor-pointer']) }}
    />

    <span class="ml-2 text-gray-600 text-sm select-none">
        {{ $label }}
    </span>
</label>
